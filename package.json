{
  "name": "ssh-gdrive-backup",
  "version": "1.1.0",
  "description": "Backups via SSH com validação de integridade, retenção segura e envio ao Google Drive.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test",
    "check": "node --check index.js && node --check sshManager.js && node --check driveManager.js && node --check googleAuthManager.js && node --check public/app.js && node --check lib/atomicJsonStore.js && node --check lib/backupIntegrity.js && node --check lib/cookies.js && node --check lib/driveQuery.js && node --check lib/scheduler.js && node --check lib/securityManager.js && node --check lib/settingsStore.js && node --check lib/validation.js && node --check test/core.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "backup",
    "ssh",
    "google-drive"
  ],
  "author": "João Paulo Vaz",
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1",
    "googleapis": "^170.1.0",
    "node-cron": "^4.2.1",
    "ssh2": "^1.17.0"
  }
}
